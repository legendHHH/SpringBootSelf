<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.legend.mybatis.mapper.IUserMapper">

    <!--resultMap:手动来配置实体属性与表字段的映射关系-->
    <resultMap id="userMap" type="com.legend.mybatis.pojo.User">
        <id property="id" column="id"></id>
        <result property="username" column="username"></result>

        <!--一对多 集合使用的标签-->
        <collection property="orderList" ofType="com.legend.mybatis.pojo.Order">
            <id property="id" column="oid"></id>
            <result property="orderTime" column="orderTime"></result>
            <result property="total" column="total"></result>
        </collection>
    </resultMap>

    <!--xml方式设置配置缓存-->
    <select id="findAll" resultMap="userMap" useCache="false" flushCache="true">
      select u.*,o.id oid,o.ordertime,o.total,o.uid from user u left join orders o on u.id=o.uid
    </select>


    <resultMap id="userRoleMap" type="com.legend.mybatis.pojo.User">
        <id property="id" column="userid"></id>
        <result property="username" column="username"></result>

        <!--多对多 集合使用的标签-->
        <collection property="roleList" ofType="com.legend.mybatis.pojo.Role">
            <id property="id" column="roleid"></id>
            <result property="roleName" column="roleName"></result>
            <result property="roleDesc" column="roleDesc"></result>
        </collection>
    </resultMap>

    <select id="findAllUserAndRole" resultMap="userRoleMap">
      select * from user u left join sys_user_role ur on ur.userid=u.id left JOIN sys_role r on r.id=ur.roleid;
    </select>

    <!--开启二级缓存-->
    <cache></cache>
</mapper>